<nav class="navbar bg-body-tertiary">
  <div class="container-fluid d-flex justify-content-between align-items-center">

    <!-- Search bar (left) -->
    <form class="d-flex" role="search">
      <input
        class="form-control me-2"
        type="search"
        placeholder="Search"
        aria-label="Search"
        />
      <button class="btn btn-outline-secondary" type="submit">
        Search
      </button>
    </form>

    <!-- Right side: Navbar text + Logout (desktop only) -->
    <div class="d-flex align-items-center gap-3">
<!--      <span class="navbar-brand mb-0 h1">My Account</span>-->

      <!-- Logout button: hidden on mobile -->
      <% if user_signed_in? %>
        <!-- Mobile Hamburger Menu on Right -->
        <button class="btn btn-link text-dark d-block d-lg-none"
                data-bs-toggle="offcanvas"
                data-bs-target="#mobileSidebar">
          <i class="bi bi-list fs-3"></i>
        </button>
        <%= button_to "Logout",
                      destroy_user_session_path,
                      method: :delete,
                      class: "btn btn-teal w-100 d-none d-lg-block" %>
      <% end %>
    </div>

  </div>
</nav>
