<%= render "shared/inner_navbar" %>
<div class="p-3">
  <h4 class="mt-3 p-3">Archived Notes</h4>

  <%= render partial: "notes/notes_grid",
             locals: {
               notes: @archived_notes,
               empty_message: "No archived notes."
             } %>
</div>

